# Hoist all @prisma/* packages to root node_modules so the custom Prisma
# generated client output (apps/api/src/generated/prisma-client) can resolve
# packages like @prisma/client-runtime-utils via Node's module lookup chain.
public-hoist-pattern[]=@prisma/*

engine-strict=true
