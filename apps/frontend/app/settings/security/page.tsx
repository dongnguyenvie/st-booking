import { SecurityPageFeature } from '@/modules/settings/security/security-page-feature';

export const metadata = {
  title: 'Security — Carousel Marketplace',
};

export default function SecuritySettingsPage() {
  return <SecurityPageFeature />;
}
