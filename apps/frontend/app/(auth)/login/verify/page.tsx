import { LoginVerifyPageFeature } from '@/modules/auth/login-verify/login-verify-page-feature';

export const metadata = {
  title: 'Verify — Carousel Marketplace',
};

export default function LoginVerifyPage() {
  return <LoginVerifyPageFeature />;
}
